/**
 * packages/ui/src/theme/tokens.types.ts — re-exports the canonical
 * ThemeTokens type from @italy-tours/config rather than redefining it, so
 * there's exactly one source of truth for a Site's design tokens.
 */
export type { ThemeTokens } from '@italy-tours/config';
